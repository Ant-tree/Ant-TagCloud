<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Ant Auto Sizing List -->
    <style name="itemListAutoSizingText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:visibility">gone</item>
        <item name="android:background">@drawable/s_item_auto_sizing_back</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLength">12</item>
        <item name="android:maxLines">1</item>
        <item name="android:minWidth">60dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:textColor">@drawable/s_item_auto_sizing_text</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="itemListAutoSizingFullText">
        <item name="android:layout_width">0dp</item>
        <item name="android:visibility">gone</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/s_item_auto_sizing_back</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLength">12</item>
        <item name="android:maxLines">1</item>
        <item name="android:minWidth">60dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:textColor">@drawable/s_item_auto_sizing_text</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
